# Copyright (c) 2026 The Syscoin Core developers
# Distributed under the MIT software license, see the accompanying
# file COPYING or http://www.opensource.org/licenses/mit-license.php.

name: Sanitizers

on:
  pull_request:
  push:
    branches:
      - '**'
    tags-ignore:
      - '**'

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CI_FAILFAST_TEST_LEAVE_DANGLING: 1
  MAKEJOBS: '-j4'
  TEST_RUNNER_PORT_MIN: 14000
  CCACHE_MAXSIZE: 300M
  CCACHE_DIR: /tmp/ccache_dir
  CCACHE_NOHASHDIR: 1

jobs:
  sanitizer:
    name: ${{ matrix.name }}
    runs-on: ubuntu-24.04
    timeout-minutes: ${{ matrix.timeout }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # SYSCOIN: Build plus both test phases exceeded three hours while
          # tests were still progressing. Run the complete native and
          # functional coverage in separate jobs with the same instrumentation.
          - name: ASan + LSan + UBSan (native)
            file_env: ./ci/test/00_setup_env_native_asan.sh
            timeout: 180
            run_unit_tests: 'true'
            run_functional_tests: 'false'
          - name: ASan + LSan + UBSan (functional)
            file_env: ./ci/test/00_setup_env_native_asan.sh
            # The cold build and long governance/NEVM fixtures need their own
            # budget; this job no longer includes the native test phase.
            timeout: 240
            run_unit_tests: 'false'
            run_functional_tests: 'true'
          - name: TSan (native)
            file_env: ./ci/test/00_setup_env_native_tsan.sh
            timeout: 180
            run_unit_tests: 'true'
            run_functional_tests: 'false'
          - name: TSan (functional)
            file_env: ./ci/test/00_setup_env_native_tsan.sh
            timeout: 180
            run_unit_tests: 'false'
            run_functional_tests: 'true'
          - name: MSan
            file_env: ./ci/test/00_setup_env_native_msan.sh
            timeout: 300
            free_disk_space: true
          - name: Fuzz smoke tests
            file_env: ./ci/test/00_setup_env_native_fuzz.sh
            timeout: 180
    env:
      FILE_ENV: ${{ matrix.file_env }}
      RUN_UNIT_TESTS: ${{ matrix.run_unit_tests }}
      RUN_FUNCTIONAL_TESTS: ${{ matrix.run_functional_tests }}
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Free disk space for the instrumented LLVM build
        if: ${{ matrix.free_disk_space == true }}
        run: |
          sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc
          docker system prune --all --force
          df -h /

      - name: Run CI configuration
        run: ./ci/test_run_all.sh
