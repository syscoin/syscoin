# Consensus and validation provenance triage

Compared Bitcoin `e4fef4ae65c68ebd34774700dc4801c24313d469` (26.0rc3) with Syscoin `d5fc88adc9992a25f26641ddb174a76e7bf64407`. Paths and line ranges below refer to the Syscoin target. This is a bounded, manual provenance review of candidate hunks in `validation`, `coins`, `consensus`, `primitives`, and `txmempool`; it is not a correctness/security review or an exhaustive finding count. No source edits, builds, or tests were performed.

“Fork-owned” below means a difference that must be preserved or consciously reconciled when merging Bitcoin. It does not assert that Syscoin originally authored every line; AuxPoW and masternode code may have earlier fork ancestry. Existing bare `// SYSCOIN` comments are credited where they clearly introduce the local block. A distant include marker is not treated as coverage for later functions.

## Confirmed missing or ambiguous boundaries

| ID | Current location | Why the provenance needs attention | Suggested marker placement / merge context |
| --- | --- | --- | --- |
| C01 | `src/validation.cpp:4942–4966`, `5347–5432` | **Ambiguous removal/disablement, high merge importance.** `DisconnectBlock` has an existing function-level SYSCOIN marker, but the local text still gives Bitcoin's historical BIP30 rationale while commenting out its two-block exception and removing the `is_bip30_exception` guard. `ConnectBlock` comments out the historical exceptions, BIP34 optimization and limit, leaving the collision loop unconditional. These are explicit departures from Bitcoin chain history, not an upstream cleanup. | Add local SYSCOIN rationale at the disabled exception in each function and immediately before the unconditional collision loop. Record in the deletion ledger that Bitcoin baseline `validation.cpp:2007–2008,2025` and `2246,2274,2304,2306,2311,2320` were removed/replaced. Explain the final behavior; do not reinsert dead BTC checks or label the entire remaining Bitcoin commentary as newly authored Syscoin code. |
| C02 | `src/primitives/block.h:24–74,123`; `src/primitives/block.cpp:11–22` | **Missing.** The block-header type now derives from `CPureBlockHeader`, carries AuxPoW, serializes its wrapper conditionally and copies it out of `CBlock`. Bitcoin has neither that type split nor AuxPoW. The header-format changes are above this file's first SYSCOIN marker. `SetAuxpow` has no marker either. | A class-level SYSCOIN explanation for the pure-header/AuxPoW split, narrow comments around changed serialization and the line 123 copy, and a marker above `SetAuxpow`. Ledger the moved/replaced BTC header fields, `GetHash`, `IsNull`, and time accessors; their disappearance from `CBlockHeader` does not mean those semantics were simply deleted. Preserve original attribution for inherited AuxPoW code. |
| C03 | `src/consensus/amount.h:19–30` | **Ambiguous.** The only explicit SYSCOIN marker introduces includes at line 8. The consensus-critical `MAX_MONEY` changes from Bitcoin's `21000000 * COIN` to `1000000000000000000LL - 1LL`, and `CAssetsMap` is added after `MoneyRange`. Product-renamed explanatory prose does not delimit these two substantive changes. | Add an adjacent SYSCOIN comment for the fork's amount bound and a separate marker for the asset-map alias. Leave ordinary `MoneyRange` implementation attributable to upstream. |
| C04 | `src/consensus/params.h:198–244` | **Missing local boundary.** Superblock cycle, seniority, subsidy intervals and PoW timing now depend on the NEVM activation height and 2.5 conversion. Bitcoin has a no-argument `PowTargetSpacing()` and a no-argument difficulty interval; the surrounding surviving PoW parameter fields are upstream. | Begin a SYSCOIN region at `SuperBlockCycle` and end after `DifficultyAdjustmentInterval`. State that the PoW accessor signatures replace the BTC no-height accessors, alongside the newly added reward/seniority helpers. |
| C05 | `src/consensus/tx_verify.cpp:167,175,186–194`; `src/consensus/tx_verify.h:26` | **Missing/partly marked.** Asset input/output maps extend the Bitcoin `CheckTxInputs` API, and asset input totals are checked in the input loop. The later `// SYSCOIN` at line 207 clearly marks output-asset validation, but does not identify the earlier signature and input accounting additions. | Annotate the extended declaration and definition, mark `hasAssets` and the asset-input loop block, and retain the existing output marker. Do not automatically attribute nearby generic fee/range-check rewrites to Syscoin without history review. |
| C06 | `src/consensus/tx_check.cpp:19–25` | **Missing.** The upstream transaction-size failure is replaced by separate committed-form and PoDA-sidecar classification using `SER_NO_PODA` and `TX_AUX_DATA_INVALID`. This references fork-only wire/identity semantics; it cannot be a plain Bitcoin backport. | SYSCOIN BEGIN/END around the replacement size-check block, explaining why the error class depends on committed size. Preserve the enclosing upstream `CheckTransaction` ownership. |
| C07 | `src/consensus/validation.h:82,151–180` | **Missing.** `BLOCK_AUX_DATA_INVALID`, rejection-cache predicates, and `HasNEVMAuxiliaryData` overloads encode NEVM/PoDA representation identity rules. Existing SYSCOIN markers cover the transaction enum additions at lines 57–59 and ChainLock at 88–89, not these independent additions. | Inline SYSCOIN annotation on the block enum value, plus a bounded region around the four helpers. Close it before the upstream weight formula commentary at line 182. |
| C08 | `src/validation.cpp:9078–9103` | **Missing local boundary.** The Bitcoin weight check is gated on the NEVM era/regtest and separates sidecar weight from committed weight; subsequent Nexus coinbase/MN-quorum version checks are also fork rules. The adjacent upstream witness/weight rationale makes an explicit boundary particularly useful. | Mark `9078–9089` as the SYSCOIN replacement of the BTC weight check and `9090–9103` as Nexus transaction-version rules. Do not encompass the preceding Bitcoin witness rationale or the following ordinary return. |
| C09 | `src/validation.cpp:958–970` | **Missing local boundary.** `MemPoolAccept::PreChecks` adds PoDA validation before missing-input/orphan classification, using `ws.mapPoDA` and fork-specific result classes. Earlier legacy SYSCOIN comments mark the distinct asset-conflict handling and do not explain this insertion. | Add a bounded SYSCOIN region around the existing sidecar-ordering comment and validation block, ending before `m_view.SetBackend` at line 972. |
| C10 | `src/validation.cpp:6676–6685,6692–6699` | **Missing.** `FindMostWorkChain` temporarily restricts candidate selection to the Geth-applied startup branch. The first SYSCOIN comment inside the function is at line 6705 for a separate BTCC quarantine mechanism. Bitcoin has neither startup-pair state nor Geth. | Mark startup-applied endpoint loading and the candidate-filter loop as two related SYSCOIN blocks, describing the temporary fork-selection constraint. Keep the underlying Bitcoin candidate iteration outside those regions. |
| C11 | `src/txmempool.cpp:984,992–1008` | **Missing local boundary.** `removeConflicts` acquires the extra `cs_main` assertion and evicts masternode registration dependants through `mapProTxRefs`. The fork-specific eviction block has no local marker; nearby preceding asset helper methods are separate code. | Mark the extra lock requirement and wrap only the `SYSCOIN_TX_VERSION_MN_REGISTER` branch. Close before the original conflict transaction's `ClearPrioritisation`/`removeRecursive` calls at lines 1009–1010. |
| C12 | `src/primitives/transaction.cpp:31–73` | **Missing.** Three NEVM version-hash validation/encoding helpers distinguish legacy and BLAKE2s versioned hash formats. The marker at line 19 introduces includes; the next marker at line 80 covers another method. The helpers lack their own boundary. | One SYSCOIN BEGIN/END region around `IsValidNEVMVersionHash`, `DecodeNEVMVersionHash`, and `EncodeNEVMVersionHash`. Give `fTestNet` at line 75 its own annotation if included in the later cleanup, rather than silently extending this region into upstream `COutPoint::ToString`. |
| C13 | `src/validation.cpp:10385–10443`; `src/validation.h:1729–1745` | **Missing.** The full-reindex helper authenticates and adopts repaired NEVM payload representations using Geth; the public repair API is likewise inserted between otherwise upstream declarations without a local marker. The helper's existing prose explains purpose but omits SYSCOIN provenance. | Add a bounded marker to the helper and another to the repair API declarations. Include local markers at its unmarked reindex call sites (`validation.cpp:10554–10561,10623–10631`) when handling that feature's complete boundary. |

## Candidates deliberately not promoted

- `src/coins.cpp` has no substantive difference in this comparison. `src/coins.h:9–12` is an include relocation/removal; `CompressedScript` and compressor types were moved into `primitives/transaction.h`. That needs ownership traced at the relocated implementation, not a claim that the ordinary UTXO implementation is unmarked Syscoin code.
- Generic `BCLog::BENCH` to `BENCHMARK` replacements, `AssertLockHeld` removals, `::cs_main` spelling changes, integer casts and include shuffles were not treated as proven Syscoin ownership. They may be later upstream changes, inherited cleanups, or local changes; this bounded pass did not assign them.
- `src/primitives/block.h:140–143` explicitly mentions `SerParams` and changes locator version handling. It is outside the AuxPoW finding; similarity to later Bitcoin serialization work must be checked independently before tagging it as fork-owned.
- `src/consensus/validation.h:57–59,88–89` already has useful local bare SYSCOIN markers. They should not become blanket “missing marker” findings merely because the inventory does not see explicit BEGIN/END pairs.
- The inventory's `validation.cpp:5676` `// END SYSCOIN` is legacy/ambiguous syntax, not proof that the preceding masternode/NEVM block lacks every marker. Normalize it only after selecting its intended start; do not manufacture a region spanning intervening upstream code.
- The recent pruning proof patch has its own marker correction. Its removed cleanup call at `validation.cpp:10306` now has a SYSCOIN relocation comment, so it is not added again here.

All 13 entries are recommendations for comments or a merge ledger, not requests to change runtime behavior. The inventory remains the complete machine-generated candidate list; this file is the manually confirmed, prioritized subset for these paths.
